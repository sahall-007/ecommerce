<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Order Failed - ShopEase</title>

    <!-- Bootstrap 5 + Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    
    <!-- Same font as your site -->
    <link href="https://fonts.googleapis.com/css2?family=Titillium+Web:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: "Titillium Web", sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .failure-card {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem 1rem;
        }
        .btn-orange {
            background-color: #ff6d00;
            border: none;
            font-weight: 600;
        }
        .btn-orange:hover {
            background-color: #e65c00;
        }
        .text-orange {
            color: #ff6d00;
        }
        footer {
            background-color: #212529;
            color: white;
            padding: 1.5rem 0;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>

    <div class="failure-card">
        <div class="text-center rounded-3 p-4 shadow-lg bg-white">
            <!-- Big Failure Icon -->
            <div class="mb-4">
                <i class="bi bi-x-circle-fill text-danger" style="font-size: 6rem;"></i>
            </div>

            <h1 class="display-5 fw-bold text-danger mb-3">Order Failed</h1>
            <p class="lead text-muted mb-2">We're sorry, but your order could not be processed</p>
            <p class="text-muted mb-4">There was an issue completing your payment. Please try again or use a different payment method.</p>

            <!-- Common Reasons (optional helpful info) -->
            <!-- <div class="text-start bg-light rounded p-4 mb-4 mx-auto" style="max-width: 600px;">
                <p class="fw-bold mb-3">Common reasons for payment failure:</p>
                <ul class="mb-0">
                    <li>Insufficient funds or card limit exceeded</li>
                    <li>Incorrect card details (number, expiry, CVV)</li>
                    <li>Expired card</li>
                    <li>Bank declined the transaction (contact your bank)</li>
                    <li>Technical issue – please try again later</li>
                </ul>
            </div> -->

            

            <div class="d-grid d-md-block gap-3">
                <a href="/checkout" class="btn btn-orange text-white px-5 py-3 me-md-3">
                    <i class="bi bi-credit-card me-2"></i>Try Payment Again
                </a>
                <a href="/allProducts" class="btn btn-outline-dark px-5 py-3">
                    <i class="bi bi-bag-heart me-2"></i>Continue Shopping
                </a>
            </div>

            <!-- <p class="text-muted mt-4 small">
                Need help? <a href="/contact" class="text-orange">Contact Support</a>
            </p> -->
        </div>
    </div>

    <!-- Sticky Footer -->
    <!-- <footer class="text-center">
        <p class="mb-0">© 2025 ShopEase. All rights reserved.</p>
    </footer> -->

    <!-- Optional: No confetti for failure -->
    <script>
        // You can add similar session check logic here if needed
        // For example, redirect if session is still open or handle failure cases
    </script>
</body>
</html>