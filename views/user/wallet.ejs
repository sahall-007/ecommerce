<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Wallet - ShopEase</title>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        html,
        body {
            height: 100%;
            /* background: #f1f4f9; */
            font-family: "Titillium Web", sans-serif;
            
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Wallet Card Gradient */
        .wallet-card {
            /* background: linear-gradient(145deg, #333, #444); */
            color: #fff;
            border-radius: 18px;
            max-width: 1024px;
            align-items: center;
        }

        /* Subtle hover effect */
        .table-row-hover:hover {
            background-color: #f7f7f7;
            transition: 0.2s ease;
        }

     

        @media (max-width: 992px) {
            .sidebar {
                position: relative;
                width: 100%;
                height: auto;
                box-shadow: none;
            }

            .content {
                margin-left: 0;
            }
        }

        .sidebar {
            background: #fff;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);

        }

        .sidebar a {
            display: block;
            padding: 12px 20px;
            font-size: 15px;
            color: #333;
            text-decoration: none;
        }

        .sidebar a:hover {
            background: #f0f3f7;
        }
    </style>
</head>

<body>

    <!-- ==================== NAVBAR ==================== -->
    <nav class="navbar navbar-expand-lg bg-black shadow-sm">
        <div class="container">
            <a class="navbar-brand text-light fw-bold d-flex align-items-center" href="/">
                <i class="bi bi-box me-2"></i> ShopEase
            </a>

            <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav mx-auto text-center mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link text-light" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link text-light" href="/allProducts">Shop List</a></li>
                </ul>

                <div class="d-flex flex-column flex-lg-row align-items-center gap-3">
                    <div class="d-flex align-items-center justify-content-center gap-4">
                        <a href="/wishlist" class="text-light"><i class="bi bi-heart"></i></a>
                        <a href="/cart" class="text-light"><i class="bi bi-cart3"></i></a>
                        <a href="/profile" class="text-light"><i class="bi bi-person-circle"></i></a>
                    </div>

                    <a href="/logout" class="btn btn-outline-light mt-3 mt-lg-0">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- ==================== USER QUICK LINKS ==================== -->
    <%- include("../partials/user/userQuickLinks.ejs") %>

    <div class="container px-5 py-5 main-container">

        <!-- ==================== WALLET CARD ==================== -->
        <div class="wallet-card shadow p-4 mb-5 mx-auto bg-dark bg-gradient">

            <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center">

                <div>
                    <p class="text-light opacity-75 mb-1">Wallet Balance</p>
                    <h1 class="fw-bold display-5">₹<%= wallet[0]?.balance || 0 %></h1>
                    <p class="opacity-75 mb-0">Available for shopping </p>
                </div>

                <div class="mt-4 mt-md-0 d-flex gap-3">                    

                    <a href="/allproducts" class="btn btn-outline-light fw-semibold px-4 py-2">
                        <i class="bi bi-cart3 me-2"></i></i>Shop now
                    </a>
                </div>

            </div>
        </div>

        <!-- ==================== TRANSACTION HISTORY ==================== -->
        <div class="bg-white rounded shadow overflow-hidden mx-auto" style="max-width: 1024px;">

            <div class="p-4 border-bottom">
                <h2 class="h4 fw-bold mb-0">Transaction History</h2>
                <p class="text-muted small mt-1">All credits and debits from your wallet</p>
            </div>

            <div class="table-responsive">
                <table class="table mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="py-3">Date</th>
                            <th class="py-3 text-center">Type</th>
                            <th class=" text-center">Description</th>
                            <th class="py-3 ">Amount</th>
                            <th class="py-3 text-center">Status</th>
                        </tr>
                    </thead>

                    <tbody id="tableData">

                        <% if(wallet?.length > 0){ %>

                            <% wallet.forEach(ele => { %>

                                <%
                                    let typeBgColor = (ele?.transactions?.transactionType == "credit") ? "bg-success" : "bg-danger"

                                    let statusBg = (ele?.transactions?.status == "Completed") ? "bg-success" : "bg-danger"
                                %>

                                <tr class="table-row-hover ">
                                    <td>
                                        <%= new Date(ele?.transactions?.date).toLocaleDateString('en-GB', {
                                                weekday: 'short',
                                                day: 'numeric',
                                                month: 'short',
                                                year: 'numeric'
                                            }) %>
                                    </td>
                                    <td class="text-center"><span class="text-light  badge <%= typeBgColor %>"><%= ele?.transactions?.transactionType %></span></td>
                                    <td class="text-center"><span class="fw-semibold"><%= ele?.transactions?.description %></span></td>
                                    <td class=" fw-semibold text-danger">&#8377; <%= ele?.transactions?.amount %></td>
                                    <td class="text-center"><span class="badge <%=statusBg%>"><%= ele?.transactions?.status %></span></td>
                                </tr>                
                            <% }) %>

                        <% } else{ %>
                            there is no transactions made
                        <% } %>

                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <div class="bg-light px-4 py-3 d-flex justify-content-between align-items-center border-top">
                <p class="mb-0 text-muted small" style="visibility: hidden;"></p>

                <div>
                    <a href="/wallet/<%= prevPage %>" class="text-light btn bg-secondary rounded border-0 px-3 py-1 <%= prevDisable %>">Previous</a>
                    <a href="/wallet/<%= nextPage %>" class="text-light btn rounded border-0 px-3 py-1 <%= nextDisable %>" style="background-color: #EF6627;">Next</a>
                </div>
            </div>

        </div>

    </div>

    <!-- ==================== FOOTER ==================== -->
    <footer class="bg-dark text-white text-center py-4 mt-auto">
        <p class="mb-0">© 2025 ShopEase. All rights reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
       

    </script>
</body>
</html>
